{% extends 'base-back/admin.html.twig' %}
{% block body %}
<div class="container">
    <h5>liste des reclamations</h5>

    {%  for message in app.flashes ('info7') %}
        <div class="alert alert-success">
            {{ message }}

        </div>
    {% endfor %}
    {%  for message in app.flashes ('info6') %}
        <div class="alert alert-warning">
            {{ message }}

        </div>
    {% endfor %}
    {%  for message in app.flashes ('info5') %}
        <div class="alert alert-danger">
            {{ message }}

        </div>
    {% endfor %}
    <table   border="1" class="table table-hover">
        <thead>
        <tr>
            <th>id</th>

            <th>name</th>
            <th>email</th>
            <th>subject</th>
            <th>message</th>
            <th>type de réclamation</th>
        </tr>
        </thead>
        <tbody>
        {% for r in reclamation  %}
        <tr>
            <td>
                {{ r.id }}
            </td>
            <td>
                {{ r.name }}
            </td>
            <td>
                {{ r.email }}
            </td>
            <td>
                {{ r.subject }}
            </td>
            <td>
                {{ r.message }}
            </td>
            <td>
                {{ r.type }}
            </td>
            <a href="{{ path('rec_list_admin',{id: r.id}) }}">

            <td><a href="{{ path('del',{id: r.id}) }}"> <button type="button" class="btn btn-outline-danger"><font size="3pt" >{% trans %}Delete{% endtrans %}</font></button></a>
            </td>
            <td><a href="{{ path('mod',{id: r.id}) }}"> <button type="button" class="btn btn-outline-warning" ><font size="3pt" >{% trans %}Update{% endtrans %}</font></button></a>
            </td>
        </tr>

        {%endfor%}
        </tbody>
    </table>
{% endblock %}


